import { Button, MainHeading } from "../components";

export const JSOperators = () => {
  // TODO: Create interactive examples for:
  // 1. Arithmetic operators (+, -, *, /, %, **)
  // 2. Comparison operators (==, ===, !=, !==, <, >, <=, >=)
  // 3. Logical operators (&&, ||, !)
  // 4. Assignment operators (=, +=, -=, *=, /=, etc.)
  // 5. Ternary operator (? :)
  // 6. Type coercion and loose vs strict equality
  // 7. Operator precedence

  return (
    <div>
      <MainHeading heading="JavaScript Operators" />
      {/* Add your interactive examples here */}
    </div>
  );
};
