import { Button, MainHeading } from "../components";

export const JSVariables = () => {
  // TODO: Create interactive examples for:
  // 1. var vs let vs const - scope differences
  // 2. Hoisting behavior for each
  // 3. Temporal Dead Zone (TDZ)
  // 4. Primitive data types (string, number, boolean, null, undefined, symbol, bigint)
  // 5. Reference data types (objects, arrays, functions)
  // 6. Type checking with typeof operator

  return (
    <div>
      <MainHeading heading="JavaScript Variables & Data Types" />
      {/* Add your interactive examples here */}
    </div>
  );
};
